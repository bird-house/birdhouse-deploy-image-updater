export TEST_ENV_CONFIG=1    # value mean nothing, true if present

export DOCKERHUB_HOST=localhost:5555
export DOCKERHUB_IMAGE_TAGS_URL=$DOCKERHUB_HOST/v2/repositories/
export EXIT_BEFORE_PR=1     # value mean nothing, true if present